name: vouchers_manager
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.0.5
  camera: ^0.11.2
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  intl: ^0.20.2
  google_mlkit_text_recognition: ^0.15.0
  http: ^1.5.0
  flutter_dotenv: ^6.0.0
  supabase_flutter: ^2.10.2
  postgrest: ^2.5.0
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.2
  uni_links: ^0.5.1 
  provider: ^6.1.1 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/receipts_data.json
    - .env
    - assets/images/
